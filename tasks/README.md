# Task memory

Create `<YYYY-MM-DD-HHMM>-<slug>.md` before a material edit and keep one active record per request.

Use sections for Goal, Plan, Blast radius, Build filter, Tests, Findings, Diff summary, Verification,
and Risks/deferred. Mark the record complete only after checks actually run are documented.
