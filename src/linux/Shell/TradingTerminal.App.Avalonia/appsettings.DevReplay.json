{
  "_comment": "Dev profile 'Dev: Replay (local DB)' (DOTNET_ENVIRONMENT=DevReplay). Skips login and replays recorded data from the local store as a live feed (10x clock). Falls back to a synthetic feed for any instrument/stream with no recorded data. PersistLiveData is off so replayed data isn't written back into the store.",
  "Dev": {
    "BypassLogin": true,
    "AutoConnectBrokers": [
      "Simulated"
    ]
  },
  "SimulatedBroker": {
    "Mode": "Replay",
    "SpeedMultiplier": 10.0,
    "Loop": true,
    "MaxGapSeconds": 2,
    "ReplayLookbackDays": 30
  },
  "MarketDataStore": {
    "PersistLiveData": false
  },
  "Plugins": {
    "TrustPolicy": "Permissive"
  }
}
