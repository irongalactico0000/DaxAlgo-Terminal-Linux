<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="TradingTerminal.App.Plugins.PluginConsentDialog"
        x:CompileBindings="False"
        Title="Unverified plugin"
        Height="440" Width="560"
        WindowStartupLocation="CenterOwner"
        CanResize="False" ShowInTaskbar="False"
        Background="{StaticResource Background.Primary}"
        Foreground="{StaticResource Text.Primary}">
  <Grid Margin="18" RowDefinitions="Auto,Auto,Auto,*,Auto">
    <TextBlock Grid.Row="0" Text="{Binding Headline}"
               FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap"
               Margin="0,0,0,10" />

    <!-- In-process plugins are not a sandbox boundary; consent stays explicit and conservative. -->
    <Border Grid.Row="1" Background="{StaticResource Background.Surface}"
            BorderBrush="{StaticResource Warning.Brush}" BorderThickness="1"
            Padding="10" Margin="0,0,0,12">
      <TextBlock Text="A strategy plugin runs inside the terminal with full access to this process. It cannot be sandboxed: it can read your broker session, your saved credentials, and your market-data store. Only run plugins from a source you trust."
                 TextWrapping="Wrap" Foreground="{StaticResource Text.Primary}" />
    </Border>

    <StackPanel Grid.Row="2" Margin="0,0,0,10">
      <TextBlock Text="{Binding PublisherText}" Margin="0,0,0,4" />
      <TextBlock Text="{Binding PathText}" Foreground="{StaticResource Text.Secondary}"
                 TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding PathText}" />
      <TextBlock Text="{Binding HashText}" Foreground="{StaticResource Text.Secondary}"
                 FontFamily="{StaticResource Font.Mono}" FontSize="11" Margin="0,2,0,0" />
    </StackPanel>

    <StackPanel Grid.Row="3">
      <TextBlock Text="What this plugin's code reaches for:"
                 FontWeight="SemiBold" Margin="0,0,0,4" />
      <ItemsControl ItemsSource="{Binding Capabilities}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <TextBlock Text="{Binding}" TextWrapping="Wrap"
                       Foreground="{StaticResource Text.Secondary}" Margin="0,1" />
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </StackPanel>

    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right"
                Margin="0,14,0,0">
      <Button Content="Don't run it" IsCancel="True" IsDefault="True"
              Click="OnRejectClicked" Padding="16,5" Margin="0,0,8,0" />
      <Button Content="Run this plugin" Click="OnConsentClicked" Padding="16,5" />
    </StackPanel>
  </Grid>
</Window>
