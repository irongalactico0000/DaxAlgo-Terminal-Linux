<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="TradingTerminal.App.Login.Forms.BybitLoginForm">
  <StackPanel>
    <TextBlock TextWrapping="Wrap"
               Foreground="{DynamicResource Text.Primary}"
               Margin="0,0,0,8">
      <Run FontWeight="SemiBold" Text="No login required. " />
      <Run Text="Bybit market data is public — click Connect to stream a real, live crypto feed (bars, L1, L2 depth and the trade tape) with no API key and no account." />
    </TextBlock>
    <Border Background="{DynamicResource Background.Surface}"
            BorderBrush="{DynamicResource Border.Brush}"
            BorderThickness="1"
            Padding="10,6">
      <TextBlock TextWrapping="Wrap"
                 FontSize="11"
                 Foreground="{DynamicResource Text.Secondary}">
        <Run FontWeight="SemiBold" Text="Heads up: " />
        <Run Text="Data only — no orders. Spot pairs by default (BTCUSDT, ETHUSDT, …), configurable under the Bybit section of appsettings.json." />
      </TextBlock>
    </Border>
  </StackPanel>
</UserControl>
