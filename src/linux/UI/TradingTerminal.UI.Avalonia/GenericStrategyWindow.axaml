<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="using:TradingTerminal.UI"
        x:Class="TradingTerminal.UI.Avalonia.GenericStrategyWindow"
        x:DataType="ui:LiveSignalStrategyViewModelBase"
        Width="760" Height="560"
        Background="{DynamicResource Background.Primary}"
        Title="{Binding StrategyDisplayName}">
  <DockPanel Margin="14">

    <StackPanel DockPanel.Dock="Top" Spacing="4">
      <TextBlock Text="{Binding StrategyDisplayName}" FontSize="18" FontWeight="Bold"
                 Foreground="{DynamicResource Text.Header}"/>
      <TextBlock Text="{Binding Status}" Foreground="{DynamicResource Text.Secondary}" TextWrapping="Wrap"/>
      <TextBlock Text="{Binding ValidationError}" Foreground="{DynamicResource Danger.Brush}" TextWrapping="Wrap"/>
    </StackPanel>

    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Spacing="8" Margin="0,10,0,0">
      <Button Content="Continue" Command="{Binding ContinueCommand}"/>
      <Button Content="Start stream" Command="{Binding StartCommand}"/>
      <Button Content="Arm / disarm" Command="{Binding ToggleAlgoCommand}"/>
      <Button Content="Stop" Command="{Binding StopCommand}"/>
      <Button Content="Clear signals" Command="{Binding ClearSignalsCommand}"/>
    </StackPanel>

    <Grid ColumnDefinitions="*,*" Margin="0,12,0,0">
      <StackPanel Grid.Column="0" Spacing="6" Margin="0,0,10,0">
        <TextBlock Text="Instrument" FontWeight="SemiBold"/>
        <TextBox Watermark="Search…" Text="{Binding InstrumentSearchText}"/>
        <ComboBox ItemsSource="{Binding Instruments}" SelectedItem="{Binding SelectedInstrument}"
                  HorizontalAlignment="Stretch">
          <ComboBox.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding DisplayName}"/>
            </DataTemplate>
          </ComboBox.ItemTemplate>
        </ComboBox>
        <TextBlock Text="Last bid / ask" FontWeight="SemiBold" Margin="0,10,0,0"/>
        <TextBlock>
          <Run Text="{Binding LastBid}"/><Run Text="  /  "/><Run Text="{Binding LastAsk}"/>
        </TextBlock>
        <TextBlock Text="{Binding TicksSeen, StringFormat='Ticks seen: {0}'}" Foreground="{DynamicResource Text.Secondary}"/>
      </StackPanel>

      <DockPanel Grid.Column="1">
        <TextBlock DockPanel.Dock="Top" Text="Signals" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <Border BorderBrush="{DynamicResource Border.Brush}" BorderThickness="1" CornerRadius="3">
          <ListBox ItemsSource="{Binding Signals}" FontSize="12">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding}"/>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </Border>
      </DockPanel>
    </Grid>
  </DockPanel>
</Window>
